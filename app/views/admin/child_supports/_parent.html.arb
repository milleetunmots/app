div class: 'card card-parent' do
  (
    div parent.admin_link(with_icon: true, target: '_blank')
  ) + (
    div parent.phone_number(with_icon: true)
  ) + (
    div parent.full_address
  ) + (
  div class: 'tags' do
    parent.tags
  end
  ) + br + (
  div class: 'tags' do
    parent.selected_modules
  end
  )
end
