<p>Si nécessaire, précisez ci-dessous une tâche à effectuer sur ce brouillon. Laissez vide pour ne pas créer de tâche.</p>

<br/><br/>

<%= semantic_form_for(@task, url: url_for([:draft, :admin, resource]), method: :put) do |f| %>

  <%= f.inputs do %>

    <%= f.input :title %>
    <%= f.input :description, input_html: { rows: 5 } %>
    <%= f.input :assignee,
                label: "Assigné à :&nbsp;&nbsp;",
                input_html: { style: 'width: 300px', data: { select2: {} } } %>

  <% end %>

  <%= f.submit 'Remettre en brouillon', style: 'width: auto; padding: 10px;' %>

<% end %>
